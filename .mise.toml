[tools]
node = "lts"
"npm:pnpm" = "latest"
"pipx:pre-commit" = "latest"

# After `mise install` on a fresh clone, run `pre-commit install` once to
# register the git hook. Not automated here because mise's hooks feature is
# experimental.
